# [TEMPLATE] bat/config
# 部署目标: ~/.config/bat/config
# AI 部署时复制此文件（仅当目标不存在），不修改本文件。

# 主题（Catppuccin Latte，与其他工具对齐）
# bat 自带 Catppuccin Latte 主题，无需额外下载
--theme="Catppuccin Latte"

# 行号 + Git 修改状态条
--style="numbers,changes,header"

# 自动检测语言
--map-syntax="*.ignore:Git Ignore"
--map-syntax=".env*:Bash"
